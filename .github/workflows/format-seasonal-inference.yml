name: Format seasonal inference

on:
  push:
    branches:
      - agent/seasonal-likelihood-inference

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/seasonal-likelihood-inference
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install black==26.5.1
      - run: python -m black src/pystarmax/seasonal_likelihood_inference.py tests/test_seasonal_likelihood_inference.py
      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pystarmax/seasonal_likelihood_inference.py tests/test_seasonal_likelihood_inference.py
          git commit -m "Format seasonal likelihood inference"
          git push
