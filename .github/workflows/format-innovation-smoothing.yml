name: Format innovation smoothing

on:
  push:
    branches:
      - agent/innovation-disturbance-smoothing

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/innovation-disturbance-smoothing
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install black==26.5.1
      - run: python -m black src/pystarmax/innovation_smoothing.py
      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pystarmax/innovation_smoothing.py
          git commit -m "Format innovation disturbance smoother"
          git push
